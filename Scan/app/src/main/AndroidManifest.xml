<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adam.android.scan">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.LOCATION_HARDWARE.camera"/>
    <uses-permission android:name="android.permission.LOCATION_HARDWARE.camera.autofocus"/>
    <uses-permission android:name="android.permission.INTERNET" />




    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".admin"></activity>
        <activity android:name=".alterUser"></activity>
        <activity android:name=".alterlocations"></activity>
        <activity android:name=".customerInsert"></activity>
        <activity android:name=".dataInsert"></activity>
        <activity android:name=".locations"></activity>
        <activity android:name=".confirmation"></activity>
        <activity android:name =".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>